# Lawthentic — One-page React site

Lightweight Vite + React + Tailwind one-page site scaffold for the Indian law firm "Lawthentic".

Quick start:

1. Install dependencies

```bash
npm install
```

2. Run dev server

```bash
npm run dev
```

This project contains simple, semantic React components under `src/components` and a Tailwind setup. The contact form is UI-only (no backend).
